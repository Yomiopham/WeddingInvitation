import { CrdtDecoder } from '../../../../json-crdt-patch/util/binary/CrdtDecoder';
import { MsgPackDecoderFast } from '../../../../json-pack/msgpack';
export declare class ViewDecoder extends MsgPackDecoderFast<CrdtDecoder> {
    protected time: number;
    constructor();
    decode(data: Uint8Array): unknown;
    protected ts(): any;
    protected cRoot(): unknown;
    cNode(): unknown;
    cObj(length: number): Record<string, unknown>;
    cTup(): unknown[];
    cArr(length: number): unknown[];
    private cArrChunk;
    cStr(length: number): string;
    cBin(length: number): Uint8Array;
}
